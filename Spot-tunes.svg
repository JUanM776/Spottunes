<svg width="820" height="220" viewBox="0 0 820 220" xmlns="http://www.w3.org/2000/svg" fill="none">

  <!-- WORDMARK -->
  <g transform="translate(40,0)">

    <!-- Spot -->
    <text x="0" y="130" font-family="Inter, SF Pro Display, Helvetica, Arial, sans-serif" font-size="64" font-weight="600" letter-spacing="0.5" fill="#FFFFFF">
      Spot
    </text>

    <!-- -Tunes -->
    <text x="170" y="130" font-family="Inter, SF Pro Display, Helvetica, Arial, sans-serif" font-size="64" font-weight="600" letter-spacing="0.5" fill="#FF6A00">
      -Tunes
    </text>

    <!-- Detalle mínimo (acento de marca) -->
    <rect x="0" y="150" width="60" height="4" rx="2" fill="#FF6A00"></rect>

  </g>

</svg>
